apiVersion: v1
kind: Service
metadata:
   name: myjavaapp-svc
   labels:
     app: myjavaapp
spec:
  type: NodePort
  ports:
  - ports: 8080
    nodeport: 3000
    protocol: TCP
  selector:
    app: myjavaapp